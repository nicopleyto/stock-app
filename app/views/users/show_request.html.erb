
<div class="card mx-auto mt-5 " style="width: 40rem;">
  <div class="card-header d-flex justify-content-evenly">
    <h4>Request</h4>
    
    <%= link_to "Declined",  new_user_path, class:"btn btn-info position-absolute top-1 end-0 mx-4 "%>
 
  </div>
  <ul class="list-group list-group-flush overflow-auto" style="min-height: 20rem; max-height: 35rem;">
    <% if @request.empty? %>
      <h4 class="text-center mt-5">There is currently no request yet.</h4>
    <% end %>
    <% @request.each do |request| %>
      <li class="list-group-item d-flex justify-content-between p-3">
      
          <%= link_to  request.email, user_path(request), class: "btn btn-outline-light text-dark"%>
          <%=  request.state %> 
          <%=  request.role %> 
          
          <div class="text-center btn-group">

            <%= link_to "Decline", update_state_user_path(request, state: "Declined"), method: :patch, class: "btn btn-danger mx-2" %>

            <%= link_to "Approve", update_state_user_path(request, state: "Approved"), method: :patch, class: "btn btn-success mx-2" %>
            
          </div>
      </li>
    <% end %>
  </ul>
</div>